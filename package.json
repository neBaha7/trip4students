{
    "name": "trip4students",
    "version": "1.0.0",
    "description": "Trip4Students — root package for Railway deployment",
    "scripts": {
        "start": "cd server && node server.js",
        "install:server": "cd server && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}